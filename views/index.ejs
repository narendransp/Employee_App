<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Employees</title>
    <link href="styles/index.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>

<body>
  <nav class="navbar navbar-dark bg-dark fixed-top shadow">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/index">Employees</a>
      <a href="/registration" class="btn btn-outline-light btn-sm">
        New Registration
      </a>
    </div>
  </nav>
  <section class="hero-section d-flex align-items-center justify-content-center text-center">
        <div class="hero-overlay">
            <h1 class="fw-bold">Employess Directory</h1>
            <p class="lead">Manage your employees efficiently</p>
            <a href="/registration" class="btn btn-success mt-3">
                Add New Employee
            </a>
        </div>
  </section>
  <div class="container page-content text-white flex-grow-1 ">
    <h3 class="text-center mb-4">Employee Details</h3>
    <div class="row g-3">
      <% employees.forEach(emp => { %>
        <div class="col-lg-3 col-md-4 col-sm-6">
          <div class="card h-100 shadow-sm">
            <div class="card-body text-dark">
              <h5 class="card-title"><%= emp.name %></h5>
              <p class="mb-1"><strong>Designation:</strong> <%= emp.designation %></p>
              <p class="mb-1"><strong>Location:</strong> <%= emp.location %></p>
              <p><strong>Salary:</strong> ₹ <%= emp.salary %></p>
              <div class="d-flex gap-2">
                <form method="POST" action="/remove/<%= emp.id %>">
                  <button class="btn btn-danger btn-sm">Remove</button>
                </form>
                <form method="GET" action="/edit/<%= emp.id %>">
                  <button class="btn btn-primary btn-sm">Edit</button>
                </form>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
  </div>
  <footer class="bg-dark text-white text-center py-3 mt-auto">
    <p class="mb-0">&copy; HRDoc 2025. All rights reserved.</p>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
