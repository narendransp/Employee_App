<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employee Registration</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="styles/registration.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-dark bg-dark fixed-top shadow">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/index">HR Doc</a>
            <a href="/registration" class="btn btn-outline-light btn-sm">
                New Employee
            </a>
        </div>
    </nav>
    
    <div class="flex-grow-1 d-flex justify-content-center align-items-center mt-5" style="padding-top: 80px;">
        <div class="col-md-6">
            <div class="card shadow-lg">
                <div class="card-header bg-dark text-white text-center">
                    <h5 class="mb-0">
                        <%= employee ? 'Edit Employee Details' : 'Employee Registration' %>
                    </h5>
                </div>
                 <div class="card-body">
                    <form method="post" action="<%= employee ? '/edit/' + employee.id : '/add' %>">
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Employee Name</label>
                            <input type="text" class="form-control" name="name" required value="<%= employee ? employee.name : '' %>">
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Designation</label>
                            <input type="text" class="form-control" name="designation" required value="<%= employee ? employee.designation : '' %>">
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Location</label>
                            <input type="text" class="form-control" name="location" required value="<%= employee ? employee.location : '' %>">
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Salary</label>
                            <input type="number" class="form-control" name="salary" required value="<%= employee ? employee.salary : '' %>">
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-success px-4">
                                <%= employee ? 'Update Employee' : 'Add Employee' %>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div> 
    </div>
<footer class="bg-dark text-white text-center py-3 mt-auto">
    <p class="mb-0">&copy; 2025 HR Doc. All rights reserved.</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
